# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# ── 敏感数据（绝不提交）────────────────────────────────
# B站登录 cookies，含账号凭证
config/cookies.json
config/bili_cookies.json
*.cookies.json

# ── 运行时生成数据（统一在 data/ 下）──────────────────
data/
# 去重状态（运行时生成）
config/processed.json

# ── 测试/临时 ─────────────────────────────────────────
test/
tmp/
*.tmp
*.part
*.ytdl

# ── 遗留代码（保留本地，不进开源仓库）────────────────
legacy/

# ── 系统 ─────────────────────────────────────────────
.DS_Store
Thumbs.db
