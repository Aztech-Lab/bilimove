# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# ── 敏感数据（绝不提交）────────────────────────────────
# B站登录 cookies，含账号凭证
config/cookies.json
config/bili_cookies.json
*.cookies.json

# ── 用户配置（本地，不进 git）────────────────────────
# 监控频道列表（每行一个 URL），模板见 channels.example.txt
channels.txt
# 本地隐私频道（优先加载，绝不进 git）
channels_local.txt

# ── 运行时生成数据（统一在 data/ 下，克隆后自动生成）──
data/
# 去重状态（运行时生成）
config/processed.json

# ── 测试/调试（本地隔离，不进 git）──────────────────
test/
tmp/
*.tmp
*.part
*.ytdl

# ── 遗留代码（保留本地，不进 git）────────────────────
legacy/

# ── 系统 ─────────────────────────────────────────────
.DS_Store
Thumbs.db
