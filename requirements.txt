# Python 依赖
PyYAML>=6.0

# 可选：可视化扩展（封面 + 动态频谱曲线）
# pip install -e ".[visualizer]"
# muvid>=0.0.34

# 系统依赖（需单独安装，不在 pip 里）：
#   - yt-dlp   （下载）  pip install yt-dlp
#   - ffmpeg   （转码）  brew install ffmpeg
#   - biliup   （上传）  pip install biliup
